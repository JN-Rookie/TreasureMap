<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="edu.feicui.treasuremap"
          tools:ignore="GoogleAppIndexingWarning,AndroidLintAllowBackup">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".TreasureApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="nIUCg4xzbzSLNOMECTjiBmRf0ivfz7xk"/>
        //key:开发者申请的key
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".treasure.HomeActivity"></activity>
        <activity android:name=".user.account.AccountActivity"></activity>
        <activity android:name=".user.login.LoginActivity"></activity>
        <activity android:name=".user.register.RegisterActivity"></activity>

    </application>

</manifest>